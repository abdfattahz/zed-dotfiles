# Zed Dotfiles

Custom Zed keybindings for a Vim-style workflow.

The keymap keeps the same leader shortcuts used in VS Code-Neovim, including
file search with `Space Space` and the project panel with `Space E`.
